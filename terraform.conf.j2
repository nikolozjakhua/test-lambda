bucket         = "{{ backend.bucket }}"
key            = "{{ backend.key }}/terraform.tfstate"
encrypt        = {{ backend.encrypt | string | lower }}
dynamodb_table = "{{ backend.dynamodb_table }}"
region         = "{{ backend.region }}"